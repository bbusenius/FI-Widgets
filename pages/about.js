import Layout from '../components/Layout'

export default function About() {
  return (
    <Layout>
      <p>
        FI Widgets is an API driven website of tools that can
        be used by the Financial Independence (FIRE) commmunity.
        The website automaticaly generates widgets based on the
        content of the FI-API which is automatically updated
        based on the existing functions in the Python based FI
        library.
      </p>
    </Layout>
  )
}
